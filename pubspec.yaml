name: ao_bi
description: Validate Angolan Identity Card
version: 0.2.1
repository: https://github.com/srnunio/ao_bi

environment:
  sdk: ^3.1.3
  flutter: ">=1.17.0"

dependencies:
  qr_code_scanner_plus: ^2.0.10+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
